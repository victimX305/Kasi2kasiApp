import { OderByTheMostRecentPipePipe } from './oder-by-the-most-recent-pipe.pipe';

describe('OderByTheMostRecentPipePipe', () => {
  it('create an instance', () => {
    const pipe = new OderByTheMostRecentPipePipe();
    expect(pipe).toBeTruthy();
  });
});
